queue_rules:
  - name: default
    merge_conditions:
      - check-success=buildbot/nix-build
    merge_method: rebase

pull_request_rules:
  - name: refactored queue action rule
    conditions:
      - base=master
      - label~=merge-queue|dependencies
    actions:
      queue:
